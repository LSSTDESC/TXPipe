launcher: local

modules: txpipe


stages:
    - name: PZPDFMLZ
      nprocess: 1
    - name: TXSelector
      nprocess: 1
    - name: TXDiagnosticMaps
      nprocess: 1
    - name: TXTwoPointFourier
      nprocess: 1
    - name: TXPhotozStack
      nprocess: 1
    # - name: TXRandomCat
    #   nprocess: 1


output_dir: ./outputs

config: test/config.yml

inputs:
    # See README for paths to download these files
    shear_catalog: ./inputs/1e5/mock_shear_catalog.fits
    photometry_catalog: ./inputs/1e5/mock_photometry_catalog.hdf
    photoz_trained_model: ./inputs/photoz_trained_model.npy

resume: False
log_dir: ./test/logs
pipeline_log: log.txt

